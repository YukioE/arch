source = /home/yukio/.cache/wal/colors-hyprland.conf

################
### MONITORS ###
################

monitor=desc:Ancor Communications Inc ASUS PB277 0x0000F421,preferred,0x0,auto
monitor=desc:Ancor Communications Inc ASUS PB277 0x0003C51C,preferred,2560x-850,auto,transform,1
monitor=eDP-1,preferred,auto,1


###################
### MY PROGRAMS ###
###################

$TERMINAL = kitty
$EDITOR = nvim
$FILEMANAGER = dolphin
$MENU = rofi -show drun -theme ~/.config/rofi/style.rasi
$COLORPICKER = hyprpicker


#################
### AUTOSTART ###
#################

exec-once = $TERMINAL
exec-once = syncthing --no-browser
exec-once = cliphist wipe | wl-paste --watch cliphist store
exec-once = waybar & awww-daemon & hypridle
exec-once = systemctl --user start hyprpolkitagent
exec-once = tailscale up && tailscale systray

#############################
### ENVIRONMENT VARIABLES ###
#############################

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = EDITOR,$EDITOR


###################
### PERMISSIONS ###
###################

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 5
    gaps_out = 5

    border_size = 2

    col.active_border = $color5
    col.inactive_border = $background

    resize_on_border = true

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 0

    active_opacity = 1.0
    inactive_opacity = 1.0

    blur {
        enabled = false
    }
}

animations {
    enabled = false
    bezier = b, 0.25, 1, 0.5, 1
    animation = windows, 1, 2, b, popin
    animation = workspaces, 1, 2, b, slide
    animation = layers, 1, 2, b, popin
    animation = fade, 1, 2, b
    animation = border, 1, 2, b
}

dwindle {
    pseudotile = true
    force_split = 2
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}


#############
### INPUT ###
#############

input {
    kb_layout = us,de
    kb_variant =
    kb_model =
    kb_options = caps:escape, grp:win_space_toggle
    kb_rules =
    accel_profile = flat

    follow_mouse = 1

    sensitivity = -0.5

    touchpad {
        natural_scroll = false
    }
}

gesture = 3, horizontal, workspace

device {
}


###################
### KEYBINDINGS ###
###################

$mainMod = SUPER

bind = $mainMod, RETURN, exec, $TERMINAL
bind = $mainMod, Q, killactive,
bind = $mainMod, E, exec, $FILEMANAGER
bind = $mainMod, T, togglefloating,
bind = $mainMod, R, exec, $MENU
bind = $mainMod, P, pseudo,
bind = $mainMod, N, layoutmsg, togglesplit
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen
bind = $mainMod CTRL, L, exec, hyprlock

bind = $mainMod, V, exec, cliphist list | rofi -dMENU -theme ~/.config/rofi/style-clipboard.rasi | cliphist decode | wl-copy
bind = CTRL SHIFT ALT, W, exec, ~/arch/wallpaper.sh
bind = $mainMod, S, exec, hyprshot -m region -z --clipboard-only

bind = CTRL, Escape, exec, killall waybar || waybar

bind = $mainMod SHIFT, C, exec, $COLORPICKER | wl-copy

# Move focus
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Swap windows
bind = $mainMod SHIFT, h, swapwindow, l
bind = $mainMod SHIFT, l, swapwindow, r
bind = $mainMod SHIFT, k, swapwindow, u
bind = $mainMod SHIFT, j, swapwindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod SHIFT, X, movecurrentworkspacetomonitor, +1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

binde = $mainMod+Shift, Right, resizeactive, 10% 0%
binde = $mainMod+Shift, Left, resizeactive, -10% 0%
binde = $mainMod+Shift, Up, resizeactive, 0% -10%
binde = $mainMod+Shift, Down, resizeactive, 0% 10%

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrule = match:class org.keepassxc.KeePassXC, float 1
windowrule {
    name = move pavucontrol
    match:class = org.pulseaudio.pavucontrol

    float = on
    size = (monitor_w*0.3) (monitor_h*0.35)
    move = (monitor_w-monitor_w*0.303) monitor_h*0.03
}
